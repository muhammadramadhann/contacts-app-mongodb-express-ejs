<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="/">Express</a>
        <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup"
            aria-expanded="false"
            aria-label="Toggle navigation"
        >
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link <% if (title == 'Home') { %> active <% } %>" href="/">Home</a>
                <a class="nav-link <% if (title == 'About') { %> active <% } %>" href="/about">About</a>
                <a
                    class="nav-link <% if (title == 'Contact' || title == 'Detail Contact' || title == 'Add New Contact' || title == 'Edit Contact') { %> active <% } %>"
                    href="/contact"
                    >Contact</a
                >
            </div>
            <form class="d-flex ms-auto" role="search">
                <input class="form-control me-2" type="search product name" placeholder="Search" aria-label="Search" />
                <button class="btn btn-primary" type="submit">Search</button>
            </form>
        </div>
    </div>
</nav>
